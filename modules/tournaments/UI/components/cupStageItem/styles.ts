import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        alignItems: 'center',
        justifyContent: 'center',
        height: 65,
        marginBottom: 10,
        borderRadius: 5
    },
    title: {
        fontSize: 24,
        fontWeight: '500',
    }
});