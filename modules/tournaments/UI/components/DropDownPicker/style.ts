import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
    selector: {
        width: '90%',
        marginTop: 10,
        height: 40,
        backgroundColor: '#2C7873'
    }
});